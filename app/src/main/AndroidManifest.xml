<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    android:installLocation="internalOnly">

    <!-- Nothing OS declares these custom system features (see
         /system_ext/etc/permissions/com.nothing.feature*.xml on-device).
         required="true" makes app stores and catalogues hide/refuse the app on
         non-Nothing devices. Sideloading ignores uses-feature by design (no
         Nothing *shared library* exists to hard-block installs with), so
         MainActivity additionally dead-ends at runtime on unsupported
         hardware. -->
    <uses-feature android:name="com.nothing.feature" android:required="true" />

    <uses-permission android:name="com.nothing.ketchum.permission.ENABLE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Both of these are the Music Visualizer, and it needs BOTH.
         audio/AudioVisualizerEngine builds a Visualizer on session 0 — the global
         output mix — and AudioFlinger gates session 0 on MODIFY_AUDIO_SETTINGS,
         on top of the RECORD_AUDIO that any Visualizer needs. Without it the
         constructor throws and the visualizer shows its "no audio" state.

         DO NOT remove MODIFY_AUDIO_SETTINGS because nothing names it. No Kotlin
         file does, and none can: it is a requirement of the framework call, not
         of app code. It was deleted once on exactly that reasoning and it broke
         music playback capture in the 3.0.0 pre-release. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <!-- ACCESS_NETWORK_STATE without INTERNET is not an oversight: the ambient
         screen draws a Wi-Fi/cellular indicator from getNetworkCapabilities
         (util/AndroidPorts.kt). This build cannot open a socket at all — see the
         github flavour's manifest for the permission it does not share. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Coarse only. Both call sites (sensors/CompassSensor, util/AndroidLocationPort)
         want a latitude/longitude for magnetic declination and sunrise maths, where
         a city-block fix and a metre-accurate one give the same answer. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <queries>
        <package android:name="com.nothing.ntessentialspace" />
        <package android:name="com.nothing.ntessentialrecorder" />
        <package android:name="com.nothing.thirdparty" />
    </queries>

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.GlyphWorks">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.DisclosureActivity"
            android:exported="false" />

        <activity
            android:name=".ui.OnboardingActivity"
            android:exported="false" />

        <activity
            android:name=".ui.design.DesignEditorActivity"
            android:exported="false" />

        <activity
            android:name=".ui.design.DesignDemoActivity"
            android:exported="false" />

        <service
            android:name=".key.EssentialKeyService"
            android:exported="true"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- No directBootAware: a bind before unlock crashes com.nothing.thirdparty. -->
        <service
            android:name=".toy.AodToyService"
            android:exported="true"
            android:icon="@drawable/toy_preview"
            android:label="@string/toy_name">
            <intent-filter>
                <action android:name="com.nothing.glyph.TOY" />
            </intent-filter>
            <meta-data
                android:name="com.nothing.glyph.toy.name"
                android:resource="@string/toy_name" />
            <meta-data
                android:name="com.nothing.glyph.toy.image"
                android:resource="@drawable/toy_preview" />
            <meta-data
                android:name="com.nothing.glyph.toy.summary"
                android:resource="@string/toy_summary" />
            <meta-data
                android:name="com.nothing.glyph.toy.introduction"
                android:value="space.linuxct.glyphworks.ui.MainActivity" />
            <meta-data
                android:name="com.nothing.glyph.toy.longpress"
                android:value="1" />
            <meta-data
                android:name="com.nothing.glyph.toy.aod_support"
                android:value="1" />
        </service>

        <receiver
            android:name=".toy.TimerAlarmReceiver"
            android:directBootAware="true"
            android:exported="false" />

        <!-- Hands a single design file to whatever the user shares it with.
             exported="false" + grantUriPermissions="true" is the whole security
             model: no app can address this provider on its own, and one URI at a
             time is granted for as long as the ACTION_SEND intent lives. The
             paths file exposes only cache/shared/, never the design store. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <service
            android:name=".key.KeyCaptureTileService"
            android:directBootAware="true"
            android:exported="true"
            android:icon="@drawable/ic_notification"
            android:label="@string/tile_label"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
            <meta-data
                android:name="android.service.quicksettings.TOGGLEABLE_TILE"
                android:value="true" />
        </service>
    </application>

</manifest>
